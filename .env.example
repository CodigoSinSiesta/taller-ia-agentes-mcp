# ===================================
# Taller IA: Variables de Entorno
# ===================================
#
# 🎯 DOS MÉTODOS DISPONIBLES:
#
# 🌐 MÉTODO 1: GitHub Codespaces (Recomendado para el taller)
#   1. Ve a GitHub: Settings → Secrets → Codespaces
#   2. Añade estos Secrets con tus claves reales:
#      - ANTHROPIC_API_KEY (para Claude)
#      - DEEPSEEK_API_KEY (para DeepSeek)
#      - LLM_PROVIDER (opcional, "claude" o "deepseek")
#   3. Crea un Codespace → ¡Las variables se cargan automáticamente!
#
# 💻 MÉTODO 2: DevContainer Local (Alternativa)
#   1. Copia este archivo: cp .env.example .env
#   2. Edita .env y completa tus claves reales (más abajo)
#   3. NUNCA hagas commit de .env (ya está en .gitignore)
#   4. Reinicia el terminal de VS Code para que se carguen las variables
#
# 🔑 ¿DÓNDE ESTÁN LAS CLAVES?
#   - Claude: https://console.anthropic.com/api/keys
#   - DeepSeek: https://platform.deepseek.com/api_keys

# ===================================
# LLM Providers - API Keys
# ===================================

# OPCIÓN 1: Anthropic Claude
# Obtén en: https://console.anthropic.com/api/keys
# Valor debe empezar con: sk-ant-
ANTHROPIC_API_KEY="sk-ant-"

# OPCIÓN 2: DeepSeek
# Obtén en: https://platform.deepseek.com/api_keys
# Valor debe empezar con: sk-
DEEPSEEK_API_KEY="sk-"

# ===================================
# LLM Provider Selection
# ===================================

# Elige qué proveedor usar por defecto: "claude" o "deepseek"
# Por defecto: claude (recomendado para empezar)
# 
# Puedes cambiar en cualquier momento:
#   export LLM_PROVIDER=deepseek && npm run agente:tareas
#   export LLM_PROVIDER=claude && npm run agente:tareas
#
# O especificar directamente en los scripts npm:
#   npm run agente:tareas:claude     # Siempre usa Claude
#   npm run agente:tareas:deepseek   # Siempre usa DeepSeek
LLM_PROVIDER="claude"

# ===================================
# Locale / Internationalization
# ===================================

# Idioma del sistema (ya configurado en devcontainer, normalmente)
LANG="es_ES.UTF-8"
LC_ALL="es_ES.UTF-8"

# ===================================
# Optional: GitHub Copilot / OpenCode
# ===================================

# Si usas GitHub Copilot en VS Code (complementario a los agentes):
# Solo necesitas autenticarte en VS Code, no hay variables de entorno
# 
# Características disponibles con Copilot:
#   - Copilot Chat (comandos naturales en VS Code)
#   - Code completion
#   - Explicación de código
#   - Refactoring asistido
#
# Más info: https://github.com/features/copilot

# ===================================
# DEBUG / Advanced Options
# ===================================

# Descomenta para ver logs detallados:
# DEBUG="taller-ia:*"

# Timeout para agentes (en ms, 0 = sin límite):
# AGENT_TIMEOUT="30000"

# ===================================
# 🎓 COMPATIBILIDAD: GitHub Secrets ↔ .env
# ===================================
#
# El sistema busca las variables en este orden:
#
# 1. Variables de entorno del sistema (primera prioridad)
# 2. GitHub Secrets (en Codespaces)
# 3. Archivo .env (en DevContainer local)
#
# ✅ Puedes usar ambos métodos simultáneamente:
#    - GitHub Secrets para Codespaces (producción)
#    - .env para desarrollo local (backup)
#
# 🔄 Migrar entre métodos:
#    - Si usas .env y quieres moverte a Codespaces:
#      1. Copia tus claves a GitHub Secrets
#      2. Elimina .env del repositorio (opcional)
#      3. Crea un nuevo Codespace
#
#    - Si usas Codespaces y quieres desarrollo local:
#      1. Copia los valores de GitHub Secrets a .env
#      2. Usa DevContainer local
#
# ⚠️ PRECAUCIÓN: No dupliques claves innecesariamente.
#    Si usas ambos métodos, GitHub Secrets tiene prioridad.
